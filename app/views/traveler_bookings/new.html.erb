<% content_for :error_messages do %>
  <!-- エラーメッセージの表示エリアをレンダリング-->
  <%= render partial: "shared/error", locals: {obj: @booking} %>
<% end %>

<%= render "tab" %>

<div class="row"> <!-- row -->
  <div class="col-xs-12 col-sm-3"> <!-- col-xs-12 col-sm-3 -->
    <%= render "thumbnail" , booking: @booking %>
  </div> <!-- col-xs-12 col-sm-3 -->
  <div class="col-xs-12 col-sm-9"> <!-- col-xs-12 col-sm-9 -->
    <%# binding.pry %>
    <%= form_for(@booking, url: {controler: "traveler_bookings", action: "create" } , html: { class: "form-horizontal"} ) do |f| %>
      <%= render "booking_schedule" , f: f %>
      <%= render "form", f: f %>

      <div class="form-group">
        <div class="col-sm-offset-3 col-sm-9 text-right">
          <%= f.submit "追加", class: "btn btn-primary" %>
        </div>
      </div>

    <% end %>
  </div><!-- col-xs-12 col-sm-9 -->
</div> <!-- row -->
<%= debug(params) if Rails.env.development? %>
