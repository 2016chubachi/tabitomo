<div class="col-sm-12">
  <dl class="dl-horizon">
    <dt>ライセンス有/無：</dt>
    <dd><%= @guide.license_flg.to_s === "1" ? "有" : "無"%></dd>
  </dl>
  <div>
    ライセンス写真表示
  </div>
  <hr>
</div>
<div class="col-sm-12">
  <dl class="dl-horizon">
    <dt>性別：</dt>
    <dd><%= @guide.member.gender.to_s === "1" ? "男" : "女"%></dd>
  </dl>
</div>
<div class="col-sm-12">
  <dl class="dl-horizon">
    <dt>経験年数：</dt>
    <dd><%= @guide.experience%></dd>
  </dl>
</div>
<div class="col-sm-12">
  <dl class="dl-horizon">
    <dt>生年：</dt>
    <dd><%= @guide.birth_year%></dd>
  </dl>
</div>
<div class="col-sm-12">
  <dl class="dl-vertical">
    <dt>ガイド都市：</dt>
    <%if @guide.guide_cities.present?%>
      <% @guide.guide_cities.each do |city| %>
        <dd><%= city.city_master.send("text_#{I18n.locale}")%></dd>
      <%end%>
    <%end%>
  </dl>
  <hr/>
</div>
<div class="col-sm-12">
  <dl class="dl-vertical">
    <dt>案内言語：</dt>
    <%if @guide.guide_languages.present?%>
      <% @guide.guide_languages.each do |language| %>
        <dd><%= "#{language.language_code.send("text_#{I18n.locale}")}:  #{language.language_skill.send("text_#{I18n.locale}")}"%></dd>
      <%end%>
    <%end%>
  </dl>
  <hr/>
</div>
<div class="col-sm-12">
  <%if !current_member.present? || current_member.traveler.present?%>
    <%= render "booking_button"%>
  <%end%>
</div>